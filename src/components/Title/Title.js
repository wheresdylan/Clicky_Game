import React from "react";
import "./Title.css";

const Title = props => (
    <div className="title">
    <h1>Zombie Clicky Game</h1>
    <p>Click on a zombie to earn points, but do not click on the same zombie twice!</p>
    </div>
);
export default Title;
